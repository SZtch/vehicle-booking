{
  "name": "Vehicle Booking - Laravel",
  "image": "mcr.microsoft.com/devcontainers/php:8.3",
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "20" },
    "ghcr.io/devcontainers/features/mysql:1": { "version": "8.0" }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libpng-dev libzip-dev libonig-dev && sudo docker-php-ext-install gd zip && composer install && cp .env.example .env && php artisan key:generate && php artisan migrate --seed && npm install && npm run build",
  "forwardPorts": [8000, 3306],
  "portsAttributes": {
    "8000": { "label": "Laravel App", "onAutoForward": "openBrowser" }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "bmewburn.vscode-intelephense-client",
        "onecentlin.laravel-blade",
        "amiralizadeh9480.laravel-extra-intellisense"
      ]
    }
  }
}
